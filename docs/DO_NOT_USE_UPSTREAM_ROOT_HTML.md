# Protected UI source rule

Do **not** use `kbilyal/ChessPublisher@5e0b377.../ChessPublisher.html` as the protected beta.34 UI source. It is an older, smaller repository-root file and fails the pinned SHA256 identity.

The source accepted for dev21/dev22 packaging is only the seven-file protected snapshot identified by `source_manifest.json` and `docs/PROTECTED_COMPONENTS.md`.
